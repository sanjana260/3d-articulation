[project]
name = "pointops"
version = "1.0"
dependencies = [
    "torch",
    "numpy",
]

[build-system]
requires = [
    "setuptools",
    "wheel",
    "torch",
]
build-backend = "setuptools.build_meta"